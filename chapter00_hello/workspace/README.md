# chapter00_hello / workspace

**この章は「プロジェクトの 0→1」なので、開始時点ではこのディレクトリは空です。**

ここにあなたが:

- `CMakeLists.txt`
- `include/greet/greeting.hpp`
- `src/greeting.cpp`
- `app/main.cpp`
- `tests/test_greeting.cpp`

を自分で作っていくのがこの章のゴールです。進め方は親ディレクトリの
[`../README.md`](../README.md) を参照してください。

行き詰まったら [`../reference/`](../reference/) を覗いて OK。

> この README は作業完了後に削除してもらって構いません（`workspace/CMakeLists.txt`
> を作った時点で、この README があっても CMake は無視します）。
